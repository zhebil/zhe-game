const constants = {
  routes: {
    truth: '/',
    never: '/never',
    questions: '/questions',
    selectPlayers: '/select-players',
    adminPage: '/admin',
    presets: '/presets',
  },
};

export default constants;
